{
  "name": "wechat-collector-frontend",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix electron run dev",
    "next:dev": "npm --prefix next run dev",
    "build:frontend": "npm --prefix next run export"
  }
}
