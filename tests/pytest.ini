# pytest 配置: 后端接口矩阵测试(隔离测试库)
[pytest]
# 允许直接 import app(backend 作为 pythonpath)
pythonpath = ../backend
# 只跑不打微信/不动鼠标的接口用例; 需要真实微信/屏幕的用 @pytest.mark.manual 标记
markers =
    manual: 需要真实微信/屏幕交互, 默认跳过(用 -m manual 手动执行)
cache_dir = .pytest_cache
testpaths = api