# 关键词采集待优化点（feature/keysearch）

> 状态：程序正式运行中，此文档仅记录待优化项，不涉及代码改动。
> 涉及流程：`gzh_query_page_init` → `gzh_query_page_article_loop`（公众号查询页文章列表循环）

---

## 待优化点 1：关键词采集的筛选设置

### 现状
- 关键词查询采集 = 按关键词搜索公众号文章列表 → 识别文章点位（判定：文本含"阅读"+数字 且 灰字白底）→ 遍历**全部**点位，逐一点击采集（`article_data_collect(collect_type=1)`）。
- 当前没有任何筛选条件：识别到的文章点位**无条件全采**。

### 期望（待确认/设计）
增加"筛选设置"，让用户决定哪些文章值得采集，候选维度：

| 维度 | 说明 | 示例 |
|---|---|---|
| 标题关键词 | 文章标题包含/排除指定词（OCR 结果里标题行做匹配） | 标题含"政务"才采 |
| 阅读量范围 | 阅读数在 [min, max] 内才采 | 阅读 ≥ 1000 |
| 点赞量范围 | 点赞数与阅读类似（需点位支持提取赞） | 点赞 ≥ 10 |
| 发布时间范围 | 见待优化点 2（时间范围） | 只采近 30 天 |

设计约束（供参考）：
- 筛选应作用于"遍历点位点击"之前（`_extract_article_points` 之后、`mouse_click` 之前），不达标点位跳过采集。
- 前端采集弹窗需增加筛选设置区（关闭/开启 + 各条件输入），随 payload 传入。
- 后端 `gzh_query_page_article_loop` 增加筛选参数（或一个筛选配置对象）。

### 前置依赖
- 点位数据里目前只有 `reads`（阅读数）；标题/赞需要 OCR 结果里额外提取，或点位改用"文章行"（标题行）作为点击对象。

---

## 待优化点 2：关键词采集兼容时间范围

### 现状
- `gzh_query_page_article_loop` 已接收 `date_start` / `date_end` 参数（与 `article_list_wait_stable` 签名一致），但**尚未使用**（点击采集时不做任何时间判断；用户此前指示"不需要时间判断"）。
- 文章点位识别已**取消时间解析**（`_TIME_RE`/`_match_article_struct`/pass B 配对均已删除，判定简化为"阅读+数字"），点位对象里 `time=None`。

### 期望（待确认/设计）
关键词采集兼容时间范围，即：按 `date_start`/`date_end` 过滤文章点位，范围外的文章跳过采集。

已知现实约束（历史踩坑记录）：
- 公众号查询页的时间格式多样且 OCR 噪声大，常见形式：
  - `2025/3/3`、`2022/10/9`（OCR 可能漏斜杠：`202210/9`、`2022109`）
  - `3/5`、`5`（月/日、日）
  - `n个月前`、`n天前`、`n小时前`（OCR 可能把"个"识别成"人"：`3人月前`）
  - 时间与阅读可能**拆成两条 OCR 条目**（需按同一行配对，行判定阈值 = box 高度 1/3）
- 相对时间（n个月前）需按"当前日期 - n"换算成绝对日期再与范围比较。
- 参考实现：`article_list_wait_stable` 的日期范围逻辑（时间点位落范围→继续/过范围→累计停止），但关键词页格式不同，需适配。

### 建议实现路径（供后续开发）
1. 恢复/重做：从 OCR 提取文章行时间（时间条目 + 阅读条目按行配对，或连读文本直接解析），转换 `n个月前/天前/小时前` → 绝对日期。
2. `gzh_query_page_article_loop` 遍历点位时：解析点位时间，`date_start`/`date_end` 范围外跳过点击采集。
3. 注意：恢复时间解析后，`_extract_article_points` 的判定应"阅读为必需、时间为可选"（时间解析不到就按"不在范围可采"或"跳过"策略，需与用户确认兜底行为）。

## 待优化点 3：新点位的自动设置需要开发

### 现状
- 关键词查询流程新增了这批点位/滚动（目前**只能手动设置**，一键设置/自动校准不覆盖）：
  | 点位/滚动 | 用途 |
  |---|---|
  | 点位 41 | 公众号查询按钮(搜一搜页) |
  | 点位 42 | 公众号查询页"发表文章更多"按钮 |
  | 点位 43 | 公众号查询页文章列表区域左上 |
  | 点位 44 | 公众号查询页文章列表区域右下 |
  | 滚动 id10 | 公众号查询文章列表滚动距离(锚点=点位43, 距离=|43.y-44.y|*0.95) |
  | 点位 18 | 文章右上角3点菜单(原有, 关键词循环滚动后也移到此) |

### 期望（待开发）
- 一键设置/自动校准流程需**自动设置 41~44 + 滚动 id10**，与旧点位(15/18/32/33 等)同样方式：自动识别公众号查询页布局并写入点位/滚动配置。
- 新点位依赖「搜一搜窗口 → 输关键词 → 公众号查询页」的完整页面状态，自动设置在模拟该状态时需同步完成（否则点位缺失会导致关键词分支无法使用）。

### 参考实现
- `backend/app/services/autosetup/`（base_points/content_points/engine）+ `routers/auto_setup.py` 的现有自动设置链路；关键词分支点位可接入同一 schema（points/scrolls 表已有结构，只需新增识别逻辑与依赖关系）。

## 待优化点 4：日志分流（API 日志与函数运行日志分开）

### 现状
- 后端日志**全落 API 侧**：`backend.log` / `error.log` 两个日志文件里都是接口/请求相关记录。
- 采集流程的函数运行日志（如 `[复制链接]`、`[db写表]`、`[存HTML]`、`[kw-loop]`、`[kw-extract]`、`[4指标]`、`[阅读数]` 等）也混在同一份里，与接口日志交织，排查时难分离。

### 期望（待开发）
- **拆成两份**：
  1. **API 日志**（接口请求/响应、路由）、
  2. **函数运行日志**（采集/识别/异步任务的运行详情）。
- 各自独立文件（如 `api.log` / `run.log` + 各自 error 文件），可独立轮转/保留。
- 现有 logger 命名已具备分流的雏形（如 `collect.article`、`collect.keysearch`、`ocr`、`perf` 等），可通过 logger name 前缀路由到不同 FileHandler；接口层日志单独接一个 handler。

## 待优化点 5：依赖系统"自动隐藏任务栏"

### 现状
- 采集流程**必须开启系统"自动隐藏任务栏"**才能正常工作：微信窗口左半屏布局、点位坐标(如文章列表区 43/44、右半屏中点收菜单、窗口宽度校验等)都建立在"任务栏不占屏幕空间"的假设上。
- 历史背景：AppEx 左半屏已改为全屏高(去掉 work_area 避让)，但仍然依赖任务栏自动隐藏，否则底部区域会被遮挡/点击偏位。

### 期望（待开发）
- **兼容任务栏存在**（不强制自动隐藏）：布局/点位坐标根据真实可用区域(work area)计算，或打包版引导用户开启的提示做在流程里。
- 候选方向：
  1. 窗口定位统一走 work_area(排除任务栏)计算左半屏；
  2. 点位自动设置时按 work_area 归一化坐标；
  3. 采集前检测任务栏状态，未自动隐藏时给出明确提示/自动校准偏移。

---

## 待优化点 6：下载/保存支持多种格式（html / pdf / txt / md / word）

### 现状
- 采集保存**仅存 html**：`data/article_data/<公众号>/<日期>_<标题>/<标题>.html`。
- 后续转 PDF / 提 txt 都是**事后手动跑外部工具**（html2pdf.py 批量转 PDF、本地脚本批量提 txt 落盘），不在采集链路内。

### 期望（待开发）
采集/下载文章时可选**保存格式**：html、pdf、txt、md、word（docx）——单选或按需多选，一次生成多份。

### 涉及环节
1. **前端**：采集弹窗/设置页增加"保存格式"选项（默认 html；多选则全部生成）；传入 payload。
2. **后端**：`article_collect.py` 的 `[存HTML]` 异步保存段扩展为"存多种格式"；各格式异步生成、失败打 error 日志。

### 各格式实现路径（供后续开发）
| 格式 | 实现方式 | 备注 |
|---|---|---|
| html | 现状保存 | 原始下载 |
| pdf | Edge/Chrome headless `--print-to-pdf`（独立工具 html2pdf.py 已验证，批量目录参数，每篇约 1~2s） | 采集完后异步批量做；注意内存（并发限制）；打包版需定位系统浏览器路径 |
| txt | HTMLParser 提取纯文本（批量 2000+ 篇约 70s，已验证） | 剔除 base64 图片后解析更快 |
| md | txt 基础上加工：`# 标题`、元信息（公众号/日期/链接）、正文；图片可保留外链或省略 | 复用 txt 提取再套模板 |
| word | 需引入 `python-docx` 依赖（或外部 pandoc 转换） | **新依赖需用户确认**；docx 结构：标题/元信息/正文段落 |

### 待确认点
1. 格式选择 UI 形态：单选 or 多选（多选则每篇生成多份文件）。
2. word 是否接受新增依赖（python-docx）或改用外部 pandoc。
3. PDF 渲染要求：动态/懒加载内容是否需要先滚动再打印。
4. **存量数据处理**：已采集的 2000+ 篇 html 是否需要"按需批量补生成"其他格式（复用现有 pdf 批量/ txt 批量脚本，非重采）。
5. 存储布局：同目录同 basename 多扩展名（推荐，txt/md/pdf/docx 与 html 并列），还是按格式分子目录。

---

## 其他备注
- 历史遗留：内存不足会导致 OCR 引擎 `bad allocation`（`ocr()` 现已打 ERROR 日志进 error.log，可区分"引擎故障"与"无文本"）。
- 关键词分支与旧分支（`article_list_wait_stable`）的 while 逻辑已对齐：截图相同跳过本轮、第2次反向回滚半屏、连续 5 次相同结束、滚动后鼠标移点位 18。