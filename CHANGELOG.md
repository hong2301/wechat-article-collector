# 更新记录 (Changelog)

所有重要变更按版本记录。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 。

## [4.0.1] - 2026-08-26

### 新增
- 静态模板库 `scripts/template_collector.db`：预置测试数据（公众号「深圳本地宝」+ 3 篇文章 + 224 条评论），打包时自动复制到 `release/data/`，装机即可体验
- CSP 安全头（仅生产注入严格策略，消除 Electron 安全隐患）
- Header 版本号动态跟随 `package.json`（升版本不再手改前端）
- README 改为面向用户版（能力介绍 + 首页截图 + 快速开始 + 整体流程）
- 文档：`docs/electron-engineering.md`（Electron 工程化待办清单）

### 变更
- 打包路径全部改为相对路径（去掉写死的绝对路径）
- 文章 HTML 默认保存目录改为数据目录下的 `data/article_data`（开发/打包一致）
- 模板数据库改为静态文件复制，不再脚本实时生成

### 修复
- `frontend/package.json` 版本号漏改（3.1.0 → 4.0.1）
- 开发模式（dev）下 `assetPrefix: "./"` 导致子路由资源 404、页面转圈 → 改为仅生产启用

## [4.0.0] - 2026-08-26

### 新增
- 列表三件套后端化：公众号/文章/评论列表**分页 + 筛选 + 排序**全部走后端 SQL（防注入白名单）
- **导出全部**：导出按钮输出筛选后的全量数据（不受当前页限制）
- **完整性校验**：点位坐标缺失、滚动距离空/0、AI 模型未配置 → 设置按钮报红 + 三页采集按钮置灰 + 原因提示
- AI 报红联动：4指标/评论采集开关强制关闭并置灰
- **任务栏隐藏**：采集弹窗打开自动隐藏任务栏（整屏采集），关闭恢复
- 日期筛选与快捷键状态记忆（刷新不丢）
- 打包全链路：`npm run build` 一键出桌面 exe（Next 导出 + Electron 壳 + PyInstaller 后端 + 模板库）
- 输入锁定（采集期间拦截人工键鼠，ESC 停止）

### 修复
- 文章列表加载死循环（setAccFilter 引用不稳 + 筛选无防抖）→ 引用稳定化 + 400ms 防抖
- 后端列表接口增加同参 400ms 去重缓存，防重复请求风暴

## [3.1.0] - 2026-08-20

> 架构重构完成：从旧版 tkinter 单文件程序迁移为 **FastAPI 后端 + Next.js 前端 + Electron 壳**。
> 旧版（3.0.x）为 tkinter 单文件 EXE，功能：公众号列表采集、文章 4 指标、阅读数、评论区采集。